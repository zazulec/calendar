import { colors } from '../colors/colors';

interface themeLight {
    background: string,
    body: string,
};

const themeLight = {
    background: colors.gray,
    body: colors.white,
};

export { themeLight };