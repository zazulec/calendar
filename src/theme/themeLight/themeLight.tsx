import { colors } from '../colors/colors';

interface themeLight {
    background: string,
    color: string,
};

const themeLight = {
    background: colors.gray,
    color: colors.white,
};

export { themeLight };