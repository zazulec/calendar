import { colors } from '../colors/colors';

interface themeDark {
    background: string,
    color: string,
};

const themeDark = {
    background: colors.black,
    color: colors.white,
};

export { themeDark };
