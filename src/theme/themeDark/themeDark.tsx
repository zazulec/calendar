import { colors } from '../colors/colors';

interface themeDark {
    background: string,
    body: string,
};

const themeDark = {
    background: colors.black,
    body: colors.white,
};

export { themeDark };
