import * as React from 'react';

function SiteBar() {
    const array = [1,3,5,6];
    const a = array.map(e => <div>{x}</div>)
    return (
        <div>
            SiteBar
            {a}
        </div>
    )
};

export { SiteBar };